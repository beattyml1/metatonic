export default class NumericMaskService {
	getMask(paramters: { 
		units?: string; 
		unitsFirst?: boolean; 
		decimalDigits?: number; 
		maxDecimalDigits?: number; 
		maxIntegerDigits?: number}) {
		// let _ = paramters;
		// let numberMask = decimalDigits ? `` : ``;
		// if (units) {
		// 	return _.unitsFirst ? `${_.units} ${numberMask}` : `${numberMask} ${_.units}`;
		// }
		return "";
	}

}