describe("DocumentStateChanges.propertyChange", () => {
	test("set the desired property", () => {
		let ds = require("StateMachines/FormState");
		let ps = require("Services/propertySelection")
		let dsc = new ds.DocumentStateChanges(ps);
	})
});