import * as React from "react";

export default class StyleEditor extends React.Component<{ styles: string, compiler: (_: string) => string }, {}> {
    render() {
        return (

        );
    }
}